# Container Environment Variables Example
# Copy this file to .env and update the values for your deployment

# Port to bind the application to
PORT=8080

# CouchDB connection URL
# For OSC CouchDB instance (replace with your actual URL and credentials)
COUCHDB_URL=https://admin:password@your-couchdb-instance.auto.prod.osaas.io

# For local CouchDB (if using docker-compose with local CouchDB)
# COUCHDB_URL=http://admin:admin123@couchdb:5984

# JWT secret for authentication (generate a secure random string)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# Node environment
NODE_ENV=production